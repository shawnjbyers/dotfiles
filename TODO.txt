git grep -E '[T]ODO|[X]XX'

Use XDG basedirs for ~/.gdbinit and other things.
gtkboard
units
python3
less
tig
.inputrc
rlwrap

Use /usr/include/sysexits.h for exit statuses on programs.

Change shell code to use tabs, not spaces.

Update nvim-manpager to failover to the system settings if vim or nvim
is not found.

Use a .editorconfig.

Make Neovim support .editorconfig.

Include bpftrace syntax highlighting for Vim and Neovim.

Make zsh completion work for units even though it's aliased to a
function.

Make /install ignore .mypy_cache directories.
